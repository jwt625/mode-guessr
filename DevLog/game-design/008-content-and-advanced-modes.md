# 008 — Content coverage and advanced game backlog

Status: canonical recipes delivered; numerical content locked behind C3. Date: 2026-09-13.

`data/examples.json` is the authoritative mapping of all 20 mandatory examples from original §26. IDs 01–05 and 20 have computed analytic answers; 06–17 and 19 require vector solves; 18 is a pending complex-field parity fixture. No geometry proxy silently substitutes for those required vector cases.

Global 20-question target counts across η buckets [0,.1), [.1,.3), [.3,.6), [.6,.8), [.8,.95), [.95,.99), [.99,1]: 2/2/3/3/4/3/3. These are pedagogical choices, not physical frequency estimates. Current cache builds an equal-coverage analytic diagnostic bank (20 per bucket), not the final session scheduler. Future scheduler draws weighted counts without replacement and reports shortages. Apply category quotas separately; do not force raw Si↔SMF to populate high-η bins through implausible parameters. Limit raw-Si↔SMF to one question per mixed speedrun. Balance HNA/SSC cases smaller, matched and larger than the endpoint.

Coverage concepts: Gaussian size/offset/tilt/polarization; SOI dimensions and alignment; fiber-chip raw/expanded interfaces; SiN confinement asymmetry; heterogeneous centroids; TE/TM and parity; selected-mode capture. Add boss cases only with sufficient visualization controls and validated physics.

| Later mode | Concrete task / acceptance | Dependency |
|---|---|---|
| Target Practice | Unlimited selected category, no category leakage, local statistics | C2/C4 |
| Blitz | 60 s active answering; interval/rank, fair final deadline | C2.3 |
| Precision | Numeric 0.001 entry; tighter solver gate and uncertainty label | C3 |
| Tolerance Engineer | Sweep x/y/tilt/width, reveal curves; unchanged anchor cached | C4 |
| Optimize Coupler | 1–3 constrained controls; optimize η or worst-case tolerance; count solves | C4 |
| Match the Mode | Geometry controls to match a fixed target; no answer before trial | C4 |
| Guess Perturbation | Nonambiguous labeled transforms; plausible distractors | C4/C5 |
| Where Did Power Go? | Same-guide orthonormal selected-mode projections and qualified remainder | C5 |
| Adaptive curriculum | Minimum evidence, error-category targeting, baseline control set | C6 |
| Scattering boss | Ask if overlap is sufficient; optional real scattering solver later | K002 |

Explanations derive from actual parameters/counterfactual overlaps: compare removing shift, size mismatch, tilt or polarization to identify the largest recoverable η. For interacting effects use “largest single improvement” rather than implying unique additive loss attribution. Phase cancellation explanations require complex fields, never merely intensity screenshots.
