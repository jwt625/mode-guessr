/** Shared flash/toggle settings so the rate and pause state persist across
 * questions and across the Gaussian <-> waveguide viewer switch. */
export const flashSettings = $state({ rateHz: 3, flashing: true });
